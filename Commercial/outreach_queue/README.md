# Outreach Queue

Drafts waiting for Herb's approval. **Nothing here sends itself** — the queue
module (`outreach_queue.py`) has no send capability by design.

Flow: VP drafts → file lands here as `pending_approval` → Herb approves in
chat → Herb sends himself.
